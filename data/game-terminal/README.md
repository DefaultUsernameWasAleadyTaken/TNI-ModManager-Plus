# Local cache (optional, gitignored)

Optional local decrypted game files for maintainers regenerating `game-command-ref/`.  
Contents under this folder (except this README) are gitignored — do not commit binaries.
